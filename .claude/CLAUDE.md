# Personal context for Claude

Global, cross-project context that applies on every machine. Project-specific
details belong in that project's own CLAUDE.md, not here.

## Who I am

- Daniel Woznicki — daniel@outset.ai
- Work at Outset (outset.ai).
- engineer

## Environment

Standardized via my dotfiles (github: this repo), symlinked into place with
`softlink.sh`. Consistent across all my machines:

- macOS + Linux; shell is zsh (bash on Linux).
- Editor: Neovim. Terminal: WezTerm. Prompt: Starship.
- Node via nvm. Search tools: ripgrep (`rg`), fd, fzf.

## Tools & services I use

- Linear (issues), Notion (docs), PostHog (analytics), Sentry (errors), Figma.

## How I like Claude to work

<!-- Corrections and preferences accumulate here. Keep them terse and durable. -->

- <e.g. be concise; show diffs before large refactors; prefer X over Y>

## Code style

- Don't write clever code. Write clear, readable code.

## Communication with colleagues

Keep messages to colleagues short and to the point. Your goal is to relay information. Do not include long-winded explanations.
